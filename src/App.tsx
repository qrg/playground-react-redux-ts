import React, { Fragment } from 'react';

import FormContainer from './containers/FormContainer';

const App = () => {
  return (
    <Fragment>
      <FormContainer />
    </Fragment>
  );
};

export default App;
